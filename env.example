# Get a free key at https://www.themoviedb.org/settings/api
# Copy this file to `.env.local` (rename) and paste your key below.
# `.env.local` is git-ignored by default in Next.js projects.
TMDB_API_KEY=
