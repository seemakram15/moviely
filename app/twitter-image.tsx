// Twitter card image — same visual as the OG card. Next.js requires
// `alt`/`size`/`contentType` to be declared per file (they can't be re-exported).
export { default, alt, size, contentType } from "./opengraph-image";
